<header class="bg-white shadow sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex justify-between items-center h-16">
        <a href="{{ url('/') }}" class="text-2xl font-bold text-blue-600 hover:text-blue-700 transition">SDN Balas Klumprik</a>
        <nav class="space-x-6 font-medium text-gray-700">
            <a href="{{ url('/') }}" class="hover:text-blue-600 transition">Home</a>
            <a href="{{ url('/profil') }}" class="hover:text-blue-600 transition">Profil</a>
            <a href="{{ url('/programs') }}" class="hover:text-blue-600 transition">Visi & Misi</a>
            <a href="{{ url('/ppdb') }}" class="hover:text-blue-600 transition">PPDB</a>
        </nav>
    </div>
</header>
