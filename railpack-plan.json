{
  "php": "8.2.*",
  "node": "22.*",
  "build": [
    "composer install --no-dev --optimize-autoloader",
    "npm ci",
    "npm run build",
    "php artisan config:clear",
    "php artisan cache:clear"
  ],
  "start": "frankenphp public/index.php --host=0.0.0.0 --port=$PORT"
}
