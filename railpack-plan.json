{
  "php": "8.2.*",
  "node": "22.*",
  "build": [
    "composer install --no-dev --optimize-autoloader",
    "npm install --omit=dev --no-audit --no-fund",
    "npm run build"
  ],
  "start": "php artisan config:clear && php artisan config:cache && php artisan route:cache && php artisan view:cache && frankenphp public/index.php --host=0.0.0.0 --port=$PORT"
}
